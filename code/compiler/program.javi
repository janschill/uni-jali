tupleval = (1, (2, 3))
Model = 0
booleanval = true

type HTML = TEXT Integer
html = TEXT 1

func myfunc x y z =
  k = x + y * z
  k
end

if booleanval
then 2
else 3

func f x y z =
  match x y z with
    | Ctor1 -> 42
    | Ctor2 -> 43
    | Ctor3 -> 44
    | Ctor4 -> 45

end
